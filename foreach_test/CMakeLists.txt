PROJECT(foreach_test)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

#AUX_SOURCE_DIRECTORY(.. SRC_LISTS)

SET(TARGET_LISTS hello world)

FOREACH(it ${TARGET_LISTS})
	ADD_EXECUTABLE(${it} ${it}.c)
	ADD_TEST(${it} ${it})
ENDFOREACH(it)
