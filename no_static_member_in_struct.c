#include <stdio.h>

struct _ABC
{
    int num;    // no static, and not num = val format
//    static int shnum;    // no static, and not num = val format

} ABC;


int 
main (void)
{
    return 0;
}
